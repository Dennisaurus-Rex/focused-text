name: focused_text
description: A text widget that focuses on a specific paragraph or phrase. Inspired by the lyrics view in Apple Music.
publish_to: 'pub.dev'
version: 1.0.0
repository: 'https://github.com/Dennisaurus-Rex/focused-text'
issue_tracker: 'https://github.com/Dennisaurus-Rex/focused-text/issues'
environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
